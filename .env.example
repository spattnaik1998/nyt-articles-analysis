# NYT Data Journalism Platform - Environment Variables

# OpenAI API Key (optional - only needed for LLM-based extraction)
# Get your API key from: https://platform.openai.com/api-keys
# Used by: src/models/extraction.py for book metadata extraction
OPENAI_API_KEY=sk-your-api-key-here

# HuggingFace Cache Directory (optional)
# Specify where to cache downloaded models
# Default: ~/.cache/huggingface
# TRANSFORMERS_CACHE=/path/to/cache

# Logging Level (optional)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# LOG_LEVEL=INFO

# Database Connection (future use)
# DATABASE_URL=postgresql://user:password@localhost:5432/nyt_db

# API Configuration (future use)
# API_HOST=0.0.0.0
# API_PORT=8000
# API_WORKERS=4

# Notes:
# 1. Copy this file to .env and fill in your values
# 2. The .env file is git-ignored for security
# 3. Only OPENAI_API_KEY is currently used (for extraction module)
# 4. All other variables are optional and have sensible defaults
